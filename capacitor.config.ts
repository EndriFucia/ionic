import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'media_player',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
